{% extends "base.html" %}
{% block content %}
<h1>\{{ app_name }}</h1>
<p>A TriStack project with an HTMX-powered web frontend.</p>
{{#if (ne orm "none")}}
<section id="items"
         hx-get="/web/items"
         hx-trigger="load"
         hx-swap="innerHTML">
    <p>Loading items…</p>
</section>
{{/if}}
{% endblock %}