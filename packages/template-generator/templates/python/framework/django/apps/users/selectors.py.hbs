"""Selectors for complex read queries."""
from .models import User


def get_user_by_id(user_id: int) -> User | None:
    """Fetch a single user by primary key."""
    try:
        return User.objects.get(pk=user_id)
    except User.DoesNotExist:
        return None